[package]
name = "persistent-todo"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "todo"
path = "src/main.rs"

[dependencies]
borsh = { version = "1.5", features = ["derive"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
